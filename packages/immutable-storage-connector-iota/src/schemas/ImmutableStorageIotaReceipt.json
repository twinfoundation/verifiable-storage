{
	"type": "object",
	"properties": {
		"@context": {
			"type": "string",
			"const": "https://schema.twindev.org/immutable-storage/",
			"description": "JSON-LD Context."
		},
		"type": {
			"type": "string",
			"const": "ImmutableStorageIotaReceipt",
			"description": "JSON-LD Type."
		},
		"milestoneIndexBooked": {
			"type": "number",
			"description": "The index of the milestone that the item was booked in to the ledger."
		},
		"milestoneTimestampBooked": {
			"type": "number",
			"description": "The timestamp of the milestone that the item was booked in to the ledger."
		},
		"network": {
			"type": "string",
			"description": "The network for the receipt."
		},
		"transactionId": {
			"type": "string",
			"description": "The transaction for the receipt."
		}
	},
	"required": [
		"@context",
		"type",
		"milestoneIndexBooked",
		"milestoneTimestampBooked",
		"network",
		"transactionId"
	],
	"additionalProperties": false,
	"description": "Receipt for the IOTA Immutable Storage connector."
}
